# Ownership of the quality gates and the machinery that enforces them.
#
# The `Gate integrity` CI job (scripts/gate-guard.sh) already blocks auto-merge
# when a PR weakens any of these unless an owner clears it with an approving review
# (an identity-bound signal, not a label an agent could self-apply). This file is
# the second layer: enable "Require review from Code Owners" on the `trunk` ruleset
# and a human review of these paths becomes mandatory too.

# CI, auto-merge, and the guard itself.
/.github/                               @TimmyMC

# The coverage / use-case / architecture gates.
/scripts/gate-guard.sh                  @TimmyMC
/crates/cli/tests/use_case_coverage.rs  @TimmyMC
/crates/cli/tests/cli_docs.rs           @TimmyMC
/crates/core/tests/architecture.rs      @TimmyMC
/ui/src/use-case-coverage.test.ts       @TimmyMC

# The intent docs the gates read.
/docs/use-cases.md                      @TimmyMC
/docs/CONSTITUTION.md                   @TimmyMC
