# Bot identity test

This file exists only to exercise the least-privilege bot PR flow:
a non-owner identity (`Zlyzart`, read-only on the base repo) opens a PR
from a fork. It carries no code change and is safe to delete.

Authored by the bot account, not the owner — verifying credential separation
for the agent pipeline (see docs/agent-pipeline.md). Delete after the test.
