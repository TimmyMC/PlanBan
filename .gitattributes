# Normalize line endings: LF in the repo (and working tree) for everything, so the
# shell scripts and git hooks stay executable on every platform and CI is consistent.
* text=auto eol=lf

# PowerShell is happiest with CRLF.
*.ps1 text eol=crlf
